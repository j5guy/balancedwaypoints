<%- include('../components/head', { title: title }) %>
<%- include('../components/nav') %>
<main class="page page-dashboard">
    <div class="page-header">
        <h1>Dashboard</h1>
        <button type="button" class="btn btn-secondary btn-sm" id="customize-dashboard-btn">Customize</button>
    </div>
    <div id="dashboard-error" class="form-error" hidden></div>

    <div class="dashboard-range-nav">
        <select id="dashboard-range-preset">
            <option value="month">This month</option>
            <option value="last3">Last 3 months</option>
            <option value="last6">Last 6 months</option>
            <option value="last12">Last 12 months</option>
            <option value="year">This year</option>
            <option value="all">All time</option>
        </select>
        <div class="month-nav" id="dashboard-month-nav">
            <button type="button" class="btn btn-secondary btn-sm" id="dashboard-prev-month-btn">&larr;</button>
            <div class="month-label" id="dashboard-month-label"></div>
            <button type="button" class="btn btn-secondary btn-sm" id="dashboard-next-month-btn">&rarr;</button>
        </div>
    </div>

    <p class="muted" style="font-size:0.85rem;">Drag a widget by its handle (⠿) to reorder it.</p>
    <div id="dashboard-widgets" class="card-grid"></div>
</main>

<div class="modal-overlay" id="customize-dashboard-overlay" hidden>
    <div class="modal-card" style="max-width:520px;">
        <h3>Customize dashboard</h3>
        <p class="muted">Reorder widgets from the dashboard itself by dragging.</p>

        <details class="customize-section" open>
            <summary>Charts</summary>
            <div id="singleton-widget-checklist"></div>
        </details>

        <details class="customize-section">
            <summary>Totals &amp; summaries</summary>
            <p class="muted" style="font-size:0.85rem;">Add one per account, plus an overall one if you'd like both.</p>
            <div id="totals-widget-list"></div>
            <div class="form-row" style="margin-top:8px;">
                <div class="form-group">
                    <label for="add-widget-type">Widget</label>
                    <select id="add-widget-type">
                        <option value="summary">Summary Card</option>
                        <option value="totalIncome">Total Income</option>
                        <option value="totalExpense">Total Expense</option>
                        <option value="netBudget">Net Budget</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="add-widget-account">Account</label>
                    <select id="add-widget-account">
                        <option value="">All accounts</option>
                    </select>
                </div>
                <div class="form-group" style="align-self:flex-end;">
                    <button type="button" class="btn btn-secondary btn-sm" id="add-widget-btn">+ Add</button>
                </div>
            </div>
        </details>

        <details class="customize-section">
            <summary>Account balances</summary>
            <p class="muted" style="font-size:0.85rem;">One per account — shows its current balance and links straight to that account's register.</p>
            <div id="balance-widget-list"></div>
            <div class="form-row" style="margin-top:8px;">
                <div class="form-group">
                    <label for="add-balance-account">Account</label>
                    <select id="add-balance-account"></select>
                </div>
                <div class="form-group" style="align-self:flex-end;">
                    <button type="button" class="btn btn-secondary btn-sm" id="add-balance-btn">+ Add</button>
                </div>
            </div>
        </details>

        <details class="customize-section">
            <summary>Account forecasts</summary>
            <p class="muted" style="font-size:0.85rem;">One per account — running balance from your chosen window in
            the past, projected out through scheduled transactions to your chosen window in the future, same as the
            register's own upcoming-schedule projection.</p>
            <div id="forecast-widget-list"></div>
            <div class="form-row" style="margin-top:8px;">
                <div class="form-group">
                    <label for="add-forecast-account">Account</label>
                    <select id="add-forecast-account"></select>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label for="add-forecast-past-amount">Past</label>
                    <input type="number" id="add-forecast-past-amount" value="10" min="1" step="1">
                </div>
                <div class="form-group">
                    <label for="add-forecast-past-unit">&nbsp;</label>
                    <select id="add-forecast-past-unit">
                        <option value="days" selected>Days</option>
                        <option value="weeks">Weeks</option>
                        <option value="months">Months</option>
                        <option value="years">Years</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="add-forecast-future-amount">Future</label>
                    <input type="number" id="add-forecast-future-amount" value="6" min="1" step="1">
                </div>
                <div class="form-group">
                    <label for="add-forecast-future-unit">&nbsp;</label>
                    <select id="add-forecast-future-unit">
                        <option value="days">Days</option>
                        <option value="weeks">Weeks</option>
                        <option value="months" selected>Months</option>
                        <option value="years">Years</option>
                    </select>
                </div>
            </div>
            <div class="form-row">
                <div class="form-group">
                    <label for="add-forecast-threshold">Low-balance threshold</label>
                    <input type="number" id="add-forecast-threshold" value="1000.00" min="0" step="0.01">
                </div>
                <div class="form-group" style="align-self:flex-end;">
                    <button type="button" class="btn btn-secondary btn-sm" id="add-forecast-btn">+ Add</button>
                </div>
            </div>
        </details>

        <div class="form-group" style="margin-top:20px;">
            <label for="widget-align">Widget text alignment</label>
            <select id="widget-align">
                <option value="left">Left</option>
                <option value="center">Center</option>
                <option value="right">Right</option>
            </select>
        </div>
        <div class="btn-row">
            <button type="button" class="btn btn-primary" id="save-widgets-btn">Save</button>
            <button type="button" class="btn btn-secondary" id="cancel-widgets-btn">Cancel</button>
        </div>
    </div>
</div>
<%- include('../components/footer', { pageScripts: ['/js/dragReorder.js', '/js/chartHover.js', '/js/dashboard.js'] }) %>

services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ${LOGS_HOST_DIR:-logs-data}:/app/logs
      - ${BACKUP_HOST_DIR:-backups-data}:/app/backups

volumes:
  logs-data:
  backups-data:

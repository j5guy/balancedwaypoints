services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - logs-data:/app/logs
      - ${BACKUP_HOST_DIR:-backups-data}:/app/backups

volumes:
  logs-data:
  backups-data:
